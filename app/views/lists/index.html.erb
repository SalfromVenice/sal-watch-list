<h1>Lists</h1>

<% @lists.each do |list| %>
  <ul>
    <li><%= link_to list.name, list_path(list) %></li>
  </ul>
<% end %>
<br>
<%= link_to "+ add list", new_list_path, class: "btn btn-outline-primary" %>
